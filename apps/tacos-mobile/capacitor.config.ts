import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'be.tacos.txnas',
  appName: 'tacos',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
