import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tacos.dev',
  appName: 'Tacos',
  webDir: '../../dist/apps/tacos-mobile',
  bundledWebRuntime: false
};

export default config;
